int command_check(char *command){
    if(strlen(command)>2){
    if(command[0]=='1'&&command[1]=='>'){
        return 1;
    }
    if(command[0]=='2'&&command[1]=='>'){
        return 2;
    }
    if (!strcmp(command, "2>&1 ")){
        return 3;
    }
}
 return 0;
}